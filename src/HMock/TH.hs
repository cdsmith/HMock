module HMock.TH
  ( makeMockable,
    makeMockableWithOptions,
    deriveMockable,
    deriveMockableWithOptions,
    deriveForMockT,
    deriveForMockTWithOptions,
    MockableOptions (..),
  )
where

import HMock.Internal.TH
